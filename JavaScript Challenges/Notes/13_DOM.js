// DOM - Document Object Model

// DOM Properties -
// - document
// - getElementById()
// - getElementsByClassName()
// - getElementsByTagName()
// - querySelector()
// - querySelectorAll()
// - innerHTML
// - textContent
// - style

// DOM Methods -
// - createElement(tagName)
// - appendChild()
// - removeChild()
// - addEventListener(event, function)
// - removeEventListener(event, function)
// - setAttribute(name, value)
// - getAttribute(name)
// - parentNode / parentElement
// - childNodes / children
// - firstChild / firstElementChild
// - lastChild / lastElementChild
// - nextSibling / nextElementSibling
// - previousSibling / previousElementSibling
// - closest(selector)

// CRUD -
// createElement(tagName) - Create new HTML element

// appendChild(node) - add element

// removeChild(node) - Remove element

// addEventListener(event, function) - Handel events

// removeEventListener(event, function) - Remove events

// setAttribute(name, value) - Set value in attribute element

// getAttribute(name) - Get value in attribute element

// innerHTML

// textContent
